<h1>Log in</h1>
<%= form_with(scope: :session, url: sessions_path, local: true ) do |f| %>
  <div class="form">
    <table>
      <tr>
        <td><%= f.label :email %></td>
        <td><%= f.email_field :email %></td>
      </tr>
      <tr>
        <td><%= f.label :password %></td>
        <td><%= f.password_field :password %></td>
      </tr>
      <tr>
        <td colspan="2"><%= f.submit "Log in" %></td>
      <tr>
    </table>
  </div>
<% end %>